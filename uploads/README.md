# Uploads

Place the source image (e.g. `vitto.png`) in this folder.
The site build reads images from here when available.

Current status: **awaiting the source image** — the upload did not reach the
sandbox filesystem (the platform-reported path `/home/user/uploads/` was empty).
